/**
 * Sets up an environment for a test by creating the right directories, config files and copying the tracking plan
 */
export declare const setupEnv: (slug: string, language: string, sdk: string, trackingPlanFile?: string, id?: string, outputPath?: string, legacyID?: string) => Promise<string>;

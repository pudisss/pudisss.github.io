import { TrackingPlanConfig } from "../config";
import { SegmentAPI } from "./api";
export declare const TRACKING_PLAN_FILENAME = "plan.json";
export declare function loadTrackingPlan(configPath: string | undefined, config: TrackingPlanConfig): Promise<SegmentAPI.TrackingPlan | undefined>;
export declare function writeTrackingPlan(configPath: string | undefined, plan: SegmentAPI.TrackingPlan, config: TrackingPlanConfig): Promise<void>;
/**
 * Loads tracking plan data for each of the tracking plan IDs in the user config.
 *
 * @param configPath path to the user config
 * @param trackingPlanConfigs array of tracking plan IDs
 * @param forceUpdate (default: false)
 * @returns Full TrackingPlan data
 */
export declare function loadTrackingPlans(apiToken: string, configPath: string, trackingPlanConfigs: TrackingPlanConfig[], forceUpdate?: boolean): Promise<SegmentAPI.TrackingPlan[]>;
/**
 * Fixes several problems with the JSONSchema returned by the API to play nice with our generator tools: quicktype and AJV
 * - Fixes the properties being nested several levels deep (to mimic the whole event structure)
 * - Fixes the IDs of the properties not being properly named for the JSONSchema draft 7 spec: removes .id, replaces with .$id
 */
export declare function sanitizeTrackingPlan(plan: SegmentAPI.TrackingPlan): SegmentAPI.TrackingPlan;
export declare function toTrackingPlanURL(workspace: string, id: string): string;

export interface WrappedError {
    isWrappedError: true;
    description: string;
    notes: string[];
    error?: Error;
}
/**
 * Helper to wrap an error w\ith a human-readable description.
 */
export declare const wrapError: (description: string, error?: Error, ...notes: string[]) => WrappedError;
export declare const isWrappedError: (error: unknown) => error is WrappedError;

import { BaseCommand } from '../base-command';
export default class Token extends BaseCommand {
    static description: string;
    static aliases: string[];
    static examples: string[];
    private formatTokenRow;
    run(): Promise<void>;
}

import { Answers, QuestionCollection } from 'inquirer';
import { SegmentAPI } from '../api';
import { FileGenerateResult, GeneratorOptions, LanguageGenerator, TemplateContext } from './types';
declare class JavascriptGenerator implements LanguageGenerator {
    id: string;
    name: string;
    extension: string;
    advancedOptions?: QuestionCollection<Answers> | undefined;
    supportedSDKs: {
        [key: string]: string;
    };
    defaultQuicktypeOptions: {
        'just-types': boolean;
    };
    constructor();
    generate: (rules: SegmentAPI.RuleMetadata[], context: TemplateContext, options: GeneratorOptions) => Promise<FileGenerateResult>;
}
export declare const javascript: JavascriptGenerator;
export {};
